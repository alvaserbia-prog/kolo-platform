{
  "_privremeno": "PRIVREMENO — ukloniti čim test build prođe. Test baza je zaglavljena na neuspeloj migraciji `20260809140000_nadzor_ishod_i_predmet` (P3009): ista migracija joj je ranije uspešno legla pod imenom `..130000..`, pa je posle preimenovanja foldera pokušala da se primeni drugi put i pukla na `type NadzorIshod already exists`. Dve `resolve` naredbe brišu neuspeli zapis i upisuju tekuće ime kao primenjeno. Guard `VERCEL_ENV != production` znači da se na produkciji NE izvršava nijedna — prod baza je uredno primila `..150000..` i ne sme se dirati.",
  "buildCommand": "if [ \"$VERCEL_ENV\" != \"production\" ] && [ -n \"$DATABASE_URL\" ]; then prisma migrate resolve --rolled-back 20260809140000_nadzor_ishod_i_predmet || true; prisma migrate resolve --applied 20260809150000_nadzor_ishod_i_predmet || true; fi; if [ -n \"$DATABASE_URL\" ]; then prisma migrate deploy; fi && npm run build",
  "regions": ["fra1"],
  "crons": [
    {
      "path": "/api/cron/zero-sum",
      "schedule": "0 6 * * *"
    },
    {
      "path": "/api/cron/nocna-emisija",
      "schedule": "0 22 * * *"
    },
    {
      "path": "/api/cron/gdpr-cistenje",
      "schedule": "0 2 1 * *"
    },
    {
      "path": "/api/cron/chat-cistenje",
      "schedule": "0 3 * * *"
    },
    {
      "path": "/api/cron/programi-revizija",
      "schedule": "0 23 * * *"
    },
    {
      "path": "/api/cron/nadzor-integriteta",
      "schedule": "0 1 * * *"
    },
    {
      "path": "/api/cron/nadzor-predmeti-cistenje",
      "schedule": "0 4 * * *"
    }
  ]
}
